require 'spec_helper'

describe Order do
  # No need to test things Rails already does...

  # let(:test_order) {Order.create}
  # let(:test_orders) { [Fabricate(:order, :status => "pending"),
  #                      Fabricate(:order, :status => "pending"),
  #                      Fabricate(:order, :status => "cancelled"),
  #                      Fabricate(:order, :status => "cancelled"),
  #                      Fabricate(:order, :status => "paid"),
  #                      Fabricate(:order, :status => "paid"),
  #                      Fabricate(:order, :status => "shipped"),
  #                      Fabricate(:order, :status => "shipped"),
  #                      Fabricate(:order, :status => "returned"),
  #                      Fabricate(:order, :status => "returned")
  #                      ] }

  # describe "status_options" do
  #   it "is an array of options" do
  #     test_order.status_options.class.to_s.should == "Array"
  #   end
  # end

  # describe ".get_all_by_status(param)" do
  #   context "status is pending" do
  #     it "returns an array" do
  #       Order.get_all_by_status("pending").class.to_s.should == "Array"
  #     end

  #     it "contains orders with the status 'pending'"
  #       Order.  
  #     end
  #   end
  # end

end
