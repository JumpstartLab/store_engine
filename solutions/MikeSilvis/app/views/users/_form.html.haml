= simple_form_for @user do |f|
  %fieldset
    %legend=legend_title
    = f.input :email
    = f.input :password
    = f.input :name
    = f.input :display_name
    = f.input :phone_number

    = f.submit :value => legend_title, :class => 'btn btn-primary controls'