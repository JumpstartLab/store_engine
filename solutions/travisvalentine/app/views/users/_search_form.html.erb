<%= form_tag orders_path, :class => "form-search", :method => "get" do %>
  <%= text_field_tag :search, params[:search], :placeholder => "Search your orders, #{current_user.first_name}" %>
  <%= submit_tag "Search" %>
<% end %>