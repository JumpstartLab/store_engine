module ShippingAddressesHelper
  # def protect_against_admin
  #   if admin? && (current_order.user.nil? || @shipping_address.user_id != current_user.id)
  #     redirect_to root_url, notice: "Sorry, we have to protect our customers' privacy"
  #   end
  # end
end
