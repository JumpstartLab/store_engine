<tr>
  <td><%= link_to "#{t('order.self')} ##{order.id}", account_order_path(order) %></td>
  <td><%= number_to_currency(order.total) %></td>
  <td><%= order.created_at.to_s(:short) %></td>
  <td><%= order.status %></td>
</tr>
