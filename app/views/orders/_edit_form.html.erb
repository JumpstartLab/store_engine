<%= form_for order do |f| %>
  <%= f.label :status %>

  <!-- TODO: STOPPED HERE
        Add in radio buttons using order.status_options
        Send them to the db.
        Yup. Yup.
        
  <!-- order.status_options %>-->

  <!-- order.status_options.each do |o| %>-->
    <!-- f.radio_button("post", ":status", "o") %>-->
  <!-- end %> -->


  <!-- f.select_tag(:status,
          options_for_select([['Pending', pending],['Cancelled', cancelled],['Paid', paid]['Shipped', shipped]['Returned', returned]])
          ) %>-->

<% end %>