<h1><%= @product.title %></h1>

<p>
  <%= @product.description %>
  <br /> <%= @product.price %>
</p>

<%= link_to "Edit", edit_product_path %>
<%= link_to "Destroy", product_path(@product), :method => :delete, confirm: "Delete \"#{@product.title}\"?" %>
<%= link_to "Back to All Products", products_path %>