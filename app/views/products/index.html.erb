<div id="products_intro">
<h2>Our Products</h2>
<% if logged_in? && current_user.is_admin? %>
<h2><%= link_to "Visit Admin View", admin_products_path %></h2>
<% end %>
</div>
<div id="products">
  <% if @products.any? %>
    <%= render :partial => "products_row" %>
  <% else %>
    <p>Worace is busy making products!</p>
  <% end %>
</div>