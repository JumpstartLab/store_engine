<h1>Your Cart </h1>

<div id="cart">

<% if @cart.cart_products.any? %>
  <%= render :partial => "carts/full_table" %>
<% else %>
  <p>Your cart is empty! Don't do Worace's Workshop like that. Go shop!</p>
<% end %>
<%= link_to "Continue Shopping", products_path %>