<h1>Your Shopping Cart.</h1>
<div id="cart">
  <ul>
    <% @cart.items.each do |item| %>
      <li>
        <%= item.title %>
      </li>
      <li id="<%= item.id %>_quant">
        <%= @cart.product_quantity(item.id) %>
      </li>
      <% end %>
  </ul>
</div>