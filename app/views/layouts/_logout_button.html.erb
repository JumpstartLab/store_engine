<% if session[:cart] && !session[:cart].empty? %>
  <%= link_to 'Logout', session_path(current_user), method: :delete, confirm: "Are you sure? This will clear your cart" %>
<% else %>
  <%= link_to 'Logout', session_path(current_user), method: :delete %>
<% end %>
