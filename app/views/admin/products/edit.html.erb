<%= form_for @product, :url => [:admin, @product] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br />

  <%= f.label :price %>
  <%= f.text_field :price %><br />

  <%= f.submit %>
<% end %>
