<% @categories.each do |category| %>
  <%= link_to category.name, admin_category_path(category) %>
  <%= link_to "Remove", admin_category_path(category), 
            :method => :delete, 
            :confirm => "Delete \"#{category.name}\"?" %><br>
<% end %>
