<%= form_for(@category, url: admin_categories_path) do |f| %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions"><br />
    <%= f.submit %>
  </div>

<% end %>