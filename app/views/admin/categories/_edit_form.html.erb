<%= form_for(@category, url: admin_category_path(@category)) do |f| %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions"><br />
    <%= f.submit %>
  </div>

<% end %>