<ul class="nav nav-pills">
<% if active_tab == 'all' %>
<li class="active">
<% else %>
<li>
<% end %>
<%= link_to "#{count} Total", admin_dashboard_path %>
</li>
<% %w(pending cancelled paid shipped returned).each do |status| %>
  <% if active_tab == status %>
  <li class="active">
  <% else %>
  <li>
  <% end %>
  <%= link_to "#{statuses[status] || 0} #{status}", admin_dashboard_path(status: status) %>
  </li>
<% end %>
</ul>
