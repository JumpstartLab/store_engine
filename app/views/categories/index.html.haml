%h1 Dashboard - Categories
= render :partial => "admin/nav"
%ul
- @categories.each do |category|
  %li
    = link_to category.name, edit_category_path(category)
    %span - 
    %span= link_to 'delete', category_path(category), :method => :delete
