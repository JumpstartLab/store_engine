%h1 Products - #{@category.name}
%br
- if @category.sale
  %span.label.label-success{:style => "font-size:15px;"} 
    All Products in this category are all on sale!
#product_holder
  = render 'products/list', :products => @products
#category_holder
  %br
  = render 'categories/list', :categories => @categories